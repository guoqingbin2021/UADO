extends: base.yaml

training:
  seeds: [11, 23, 37, 53, 71, 89, 107]

runtime:
  gpu_ids: [0, 1, 2, 3]
  workers_per_gpu: 4
  workers_per_gpu_by_id: {"0": 4, "1": 4, "2": 4, "3": 1}
  gpu_memory_fraction_per_worker: 0.22
  resume: true
  retry_count: 1
  output_dir: output/formal
